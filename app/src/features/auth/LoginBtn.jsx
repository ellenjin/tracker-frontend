// Purpose: Trigger login submission.
// Events: onClick -> call login handler from parent.
// Imports: none.
// Routes: None directly, handled by parent form.

const LoginBtn = () => {
  return <button type="submit">Login</button>;
};

export default LoginBtn;
